# Chainsaw policy configuration
policy:
  # Minimum severity to fail the scan (critical, high, medium, low, none)
  fail-on: high

  # CVE/GHSA IDs to ignore (accepted risk)
  # Supports both bare strings and time-bounded rules:
  ignore: []
    # - CVE-2024-1234
    # - id: CVE-2024-5678
    #   expires: "2026-12-31"
    #   reason: "Accepted risk - no exploitable path in our usage"

  # Licence policy
  licences:
    deny-list:
      - GPL-3.0-only
      - AGPL-3.0-only
      - AGPL-3.0-or-later

# CRA compliance configuration
cra:
  manufacturer: "chainsaw-dev"
  security-contact: "security@example.com"
  support-end-date: "2028-05-28"
  csirt-contact: "security@example.com"

# Supply chain policy
supply-chain:
  min-pinning-score: 0
  require-sha-pins: false
